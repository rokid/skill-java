info:
  app:
    version: 1.0.0

spring:
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  profiles:
    active: local
  application:
    name: skill-demo

server:
  servlet:
    context-path: /skill-demo
  port: 8080

